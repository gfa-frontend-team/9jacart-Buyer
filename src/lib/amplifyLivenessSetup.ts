/**
 * NeoCash BNPL liveness step lazy-loads Amplify FaceLivenessDetector.
 * Import styles once at app bootstrap (npm peer path — not CDN assetPrefix).
 */
import "@aws-amplify/ui-react/styles.css";
import "@aws-amplify/ui-react-liveness/styles.css";
